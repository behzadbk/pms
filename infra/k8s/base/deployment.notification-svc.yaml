# الگوی مشترک Deployment + Service برای هر میکروسرویس NestJS.
# برای سرویس جدید: کپی کنید، مقادیر notification-svc و 3006 را جایگزین کنید،
# و نام فایل را به deployment.<service-name>.yaml تغییر دهید.
# نمونه پرشده: deployment.identity-svc.yaml در همین پوشه.

apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-svc
  namespace: pms-prod
  labels:
    app: notification-svc
spec:
  replicas: 2 # حداقل ۲ Pod برای HA؛ HorizontalPodAutoscaler می‌تواند این را بالاتر ببرد
  selector:
    matchLabels:
      app: notification-svc
  template:
    metadata:
      labels:
        app: notification-svc
    spec:
      containers:
        - name: notification-svc
          image: ghcr.io/pms-saas/notification-svc:__IMAGE_TAG__ # در CI/CD با git-sha جایگزین می‌شود
          ports:
            - containerPort: 3006
          envFrom:
            - configMapRef:
                name: pms-shared-config
            - secretRef:
                name: pms-secrets
          env:
            - name: PORT
              value: "3006"
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 3006
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health/live
              port: 3006
            initialDelaySeconds: 10
            periodSeconds: 15
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false  # NestJS به /tmp نیاز دارد؛ برای سخت‌گیری بیشتر با emptyDir mount کنید
---
apiVersion: v1
kind: Service
metadata:
  name: notification-svc
  namespace: pms-prod
spec:
  type: ClusterIP
  selector:
    app: notification-svc
  ports:
    - port: 3006
      targetPort: 3006
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: notification-svc-hpa
  namespace: pms-prod
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: notification-svc
  minReplicas: 2
  maxReplicas: 8
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
