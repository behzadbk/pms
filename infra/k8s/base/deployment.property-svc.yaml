# الگوی مشترک Deployment + Service برای هر میکروسرویس NestJS.
# برای سرویس جدید: کپی کنید، مقادیر property-svc و 3002 را جایگزین کنید،
# و نام فایل را به deployment.<service-name>.yaml تغییر دهید.
# نمونه پرشده: deployment.identity-svc.yaml در همین پوشه.

apiVersion: apps/v1
kind: Deployment
metadata:
  name: property-svc
  namespace: pms-prod
  labels:
    app: property-svc
spec:
  replicas: 2 # حداقل ۲ Pod برای HA؛ HorizontalPodAutoscaler می‌تواند این را بالاتر ببرد
  selector:
    matchLabels:
      app: property-svc
  template:
    metadata:
      labels:
        app: property-svc
    spec:
      containers:
        - name: property-svc
          image: ghcr.io/pms-saas/property-svc:__IMAGE_TAG__ # در CI/CD با git-sha جایگزین می‌شود
          ports:
            - containerPort: 3002
          envFrom:
            - configMapRef:
                name: pms-shared-config
            - secretRef:
                name: pms-secrets
          env:
            - name: PORT
              value: "3002"
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 3002
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health/live
              port: 3002
            initialDelaySeconds: 10
            periodSeconds: 15
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false  # NestJS به /tmp نیاز دارد؛ برای سخت‌گیری بیشتر با emptyDir mount کنید
---
apiVersion: v1
kind: Service
metadata:
  name: property-svc
  namespace: pms-prod
spec:
  type: ClusterIP
  selector:
    app: property-svc
  ports:
    - port: 3002
      targetPort: 3002
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: property-svc-hpa
  namespace: pms-prod
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: property-svc
  minReplicas: 2
  maxReplicas: 8
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
