-- ============================================================================
-- guard-service 002 — ستون photo_url برای مرسولات
-- ----------------------------------------------------------------------------
-- parcels.controller.ts هنگام ثبت مرسوله در photo_url می‌نویسد اما جدول 001 این
-- ستون را نداشت؛ نتیجه: «ثبت مرسوله» (هسته‌ی پنل نگهبانی) همیشه خطای 500 می‌داد.
-- ============================================================================
ALTER TABLE guard.parcels ADD COLUMN IF NOT EXISTS photo_url text;
